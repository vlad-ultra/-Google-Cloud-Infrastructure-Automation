name: Test Trigger
# Temporary workflow to test workflow_dispatch

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: 'Test input'
        required: false
        default: 'test'
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo "Test workflow triggered"
